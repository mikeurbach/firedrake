import firedrake as fd

sockets = {}

def acc(socket, string):
    if not sockets[socket]:
        sockets[socket] = []

    sockets[socket].append

def callback(socket):
    socket.ondata = acc
    socket.onend = dump

fd.onconnection(8080, callback)
